1.任务提交后应该显示处理的队列，进度,任务完成的保存路径应该提供一个指定界面
2.正常非 dev 模式下要有登录界面和任务管理，用户登录后可进行充值积分，但具体涉及到的金融充值暂不开发，此时登录后自带Bearer token，不显示该界面
3.任务完成后应提供可下载的 excle 表格
4.未来将该功能一键 pip（这可能意味着开源），gpt 等 mcp 可调用该功能，或者撰写一个 skills，但核心提取任务在我们这进行
